#ifndef ENUMS_H
#define ENUMS_H

enum actions { no_action, squared_lines, rgbfilter, rotate, mirror, diag_mirror, square_rhombus };
enum to_display { dont_display, display };
enum to_fill { dont_fill, fill };
enum color { red, green, blue };

#endif